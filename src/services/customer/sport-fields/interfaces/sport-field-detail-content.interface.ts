export interface ISportFieldDetailContent {
  text: {
    distance: string;
    location: string;
    owner: string;
    includes: string;
    hour: string;
  };
  buttons: {
    reservation: string;
  };
}
